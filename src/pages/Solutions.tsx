
import React from "react";
import { Navigate } from "react-router-dom";

const Solutions = () => {
  // Redirect to free resources page
  return <Navigate to="/solutions/free-resources" replace />;
};

export default Solutions;
